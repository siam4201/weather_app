name: weather_app
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.0
  geocoding: ^4.0.0
  geolocator: ^14.0.2
  http: ^1.6.0
  lottie: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: "^0.14.3"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/—Pngtree—flat map icon depicting sun_12480503.jpg"

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env
